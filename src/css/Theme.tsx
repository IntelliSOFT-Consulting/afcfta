import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#F5A729",
    },
    secondary: {
      main: "#F5A729",
    },
  },
});

export default theme;
